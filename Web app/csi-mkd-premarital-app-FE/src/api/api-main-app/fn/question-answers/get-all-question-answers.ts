/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { QuestionAnswersResponseDto } from '../../models/question-answers-response-dto';

export interface GetAllQuestionAnswers$Params {
  page?: number;
  pageSize?: number;
}

export function getAllQuestionAnswers(http: HttpClient, rootUrl: string, params?: GetAllQuestionAnswers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<QuestionAnswersResponseDto>>> {
  const rb = new RequestBuilder(rootUrl, getAllQuestionAnswers.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('pageSize', params.pageSize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<QuestionAnswersResponseDto>>;
    })
  );
}

getAllQuestionAnswers.PATH = '/api/question-answers';
