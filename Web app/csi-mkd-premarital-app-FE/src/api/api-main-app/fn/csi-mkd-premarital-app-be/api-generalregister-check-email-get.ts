/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CheckEmailResponseDto } from '../../models/check-email-response-dto';

export interface ApiGeneralregisterCheckEmailGet$Params {
  email: string;
}

export function apiGeneralregisterCheckEmailGet(http: HttpClient, rootUrl: string, params: ApiGeneralregisterCheckEmailGet$Params, context?: HttpContext): Observable<StrictHttpResponse<CheckEmailResponseDto>> {
  const rb = new RequestBuilder(rootUrl, apiGeneralregisterCheckEmailGet.PATH, 'get');
  if (params) {
    rb.query('email', params.email, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CheckEmailResponseDto>;
    })
  );
}

apiGeneralregisterCheckEmailGet.PATH = '/api/generalregister/check-email';
