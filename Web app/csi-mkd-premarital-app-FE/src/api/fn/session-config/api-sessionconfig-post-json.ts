/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CreateUpdateSessionDto } from '../../models/create-update-session-dto';
import { SessionConfigurationDto } from '../../models/session-configuration-dto';

export interface ApiSessionconfigPost$Json$Params {
      body?: CreateUpdateSessionDto
}

export function apiSessionconfigPost$Json(http: HttpClient, rootUrl: string, params?: ApiSessionconfigPost$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<SessionConfigurationDto>> {
  const rb = new RequestBuilder(rootUrl, apiSessionconfigPost$Json.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/*+json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<SessionConfigurationDto>;
    })
  );
}

apiSessionconfigPost$Json.PATH = '/api/sessionconfig';
